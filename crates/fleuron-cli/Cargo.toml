[package]
name = "fleuron-cli"
description = "Placeholder reservation of the fleuron name: paged-media layout engine for book-shaped documents. Engine under development — see repository."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true

[[bin]]
name = "fleuron"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
serde_json = "1"
fleuron = { path = "../fleuron" }
fleuron-markdown = { path = "../fleuron-markdown" }

[dev-dependencies]
# The e2e checks the fixture PDF against a digest; nothing ships with it.
sha2 = "0.10"
