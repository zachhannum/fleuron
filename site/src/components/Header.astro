---
import Search from 'virtual:starlight/components/Search';
import SocialIcons from 'virtual:starlight/components/SocialIcons';
import ThemeSelect from 'virtual:starlight/components/ThemeSelect';
import config from 'virtual:starlight/user-config';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const shouldRenderSearch =
  config.pagefind ||
  config.components.Search !== '@astrojs/starlight/components/Search.astro';
---

<div class="f-bar"><i></i><i></i><i></i><i></i></div>

<div class="header">
  <div class="title-wrapper sl-flex">
    <a href={`${base}/`} class="site-title sl-flex">
      <span translate="no">fleuron</span>
      <span class="f-mark" aria-hidden="true">&#10086;</span>
    </a>
  </div>
  <div class="sl-flex print:hidden">
    {shouldRenderSearch && <Search />}
  </div>
  <div class="sl-hidden md:sl-flex print:hidden right-group">
    <a class="chrome-link" href={`${base}/api/fleuron/`}>API</a>
    <div class="sl-flex social-icons">
      <SocialIcons />
    </div>
    <ThemeSelect />
  </div>
</div>

<style>
  /* Starlight gives the bar its padding; the colour bar above runs
     the full width, so this row takes it back. */
  .header {
    display: flex;
    gap: var(--sl-nav-gap);
    justify-content: space-between;
    align-items: center;
    height: calc(100% - 32px);
    padding: var(--sl-nav-pad-y) var(--sl-nav-pad-x);
  }

  .title-wrapper {
    overflow: clip;
    padding: 0.25rem;
    margin: -0.25rem;
    min-width: 0;
  }

  .site-title {
    align-items: baseline;
    gap: 0.1rem;
    font-family: var(--f-display);
    font-variation-settings: var(--f-display-var);
    font-size: 1.45rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    text-transform: lowercase;
    color: var(--f-ink);
    text-decoration: none;
    white-space: nowrap;
    min-width: 0;
  }

  .site-title .f-mark {
    font-size: 1.2rem;
  }

  .chrome-link {
    padding-bottom: 2px;
    border-bottom: 3px solid transparent;
    font-family: var(--f-chrome);
    font-size: 0.76rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--f-ink);
    text-decoration: none;
  }

  .chrome-link:hover {
    border-bottom-color: var(--f-ink-c);
  }

  .right-group,
  .social-icons {
    gap: 1rem;
    align-items: center;
  }

  .social-icons::after {
    content: '';
    height: 1.5rem;
    border-inline-end: 3px solid var(--f-rule);
  }

  @media (min-width: 50rem) {
    :global(:root[data-has-sidebar]) {
      --__sidebar-pad: calc(2 * var(--sl-nav-pad-x));
    }
    :global(:root:not([data-has-toc])) {
      --__toc-width: 0rem;
    }
    .header {
      height: calc(100% - 32px);
      --__sidebar-width: max(
        0rem,
        var(--sl-content-inline-start, 0rem) - var(--sl-nav-pad-x)
      );
      --__main-column-fr: calc(
        (
            100% + var(--__sidebar-pad, 0rem) -
              var(--__toc-width, var(--sl-sidebar-width)) -
              (2 * var(--__toc-width, var(--sl-nav-pad-x))) -
              var(--sl-content-inline-start, 0rem) - var(--sl-content-width)
          ) / 2
      );
      display: grid;
      grid-template-columns:
        minmax(
          calc(
            var(--__sidebar-width) +
              max(0rem, var(--__main-column-fr) - var(--sl-nav-gap))
          ),
          auto
        )
        1fr
        auto;
      align-content: center;
    }
  }
</style>
